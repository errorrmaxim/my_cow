#ifndef CALCUL_H
#define CALCUL_H
#include <cmath>

class calcul
{
    double a;
    double b;
public:
    calcul(double, double);
    double sum();
    double dif();
    double mult();
    double div();

};

#endif // CALCUL_H
